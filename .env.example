# Configuration n8n

# Mot de passe pour l'interface n8n
N8N_PASSWORD=changeme123

# Domaine (pour production avec HTTPS)
# N8N_HOST=n8n.votre-domaine.com
# N8N_PROTOCOL=https

# Pour développement local
N8N_HOST=localhost
N8N_PROTOCOL=http

# API Keys (à configurer dans n8n, pas ici)
# OPENAI_API_KEY=sk-...
# Ces clés doivent être configurées dans l'interface n8n (Credentials)
